ESTUDIANTE: Edwar Aguilar Ledezma

CONTENIDO: Contiene tres carpetas(src, out, DocumentosRegistros), dos archivos.dat
            *   En la carpeta src esta el area de desarrollo
            *   En la carpeta out esta el area de production
            *   En la carpeta DocumentosRegistros estan tres carpetas de nombre caso1, caso2, caso3
                *   En la subcarpeta caso1 se encuentra el archivo registros.dat para el caso 1 donde
                    tiene que tener registrado de 1 o 2 dias seguidos con sus respectivos sintomas.
                *   En la subcarpeta caso2 se encuentra el archivo registros.dat para el caso 2 donde
                    tiene que tener registrado de 3 fechas seguidas con sus respectivos sintomas.
                *   En la subcarpeta caso3 se encuentra el archivo registros.dat para el caso 3 donde
                    tiene que tener registrado de 4 a 8 registros de fechas seguidas.
                *   En la subcarpeta caso4 se encuentra el archivo registros.dat para un caso especifico
                    donde tiene los registros de dos fechas discontinuas.

                Actualmente esta con el archivo registros.dat del caso3, y el archivo fasse.dat se crea
                en la carpeta src