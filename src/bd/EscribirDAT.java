package bd;

public class EscribirDAT {

    /*private final String PAQUETE;

    private boolean estado;

    public EscribirDAT(String ruta){
        estado = false;
    }

    public boolean getEstado() {
        return estado;
    }

    public void escribir(String paquete,List<List<String>> lDatos) {
        for(List<String> lcad : lDatos) {
            if(cadenaNula(paquete)) {
                escribir
            }
        }
    }

    public void escribir(String paquete, String nombreSintoma, String tipo) {
        //boolean guardado = false;
        Sintoma sintoma = null;
        Sintomas sintomas = this.getSintomasArchivo();
        ObjectOutputStream file = null;

        try {
            Class<?> cl = Class.forName(this.paquete + "." + tipo);
            Constructor<?> constructor = cl.getConstructor(String.class);
            sintoma = (Sintoma)constructor.newInstance(nombreSintoma);
            sintomas.add(sintoma);
            file = new ObjectOutputStream(new FileOutputStream(this.archivoSintomas));
            file.writeObject(sintomas);
            file.close();
            estado = true;
        } catch (Exception var9) {
        }
    }

    private boolean cadenaNula(String cadena) {
        return (cadena.equals(null) || cadena.length() == 0);
    }*/

}
